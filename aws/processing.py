def parse_df(filename):
    return 12


def find_month_data(month):
    return


def parse_all_months():
    input
    bucket
    return


def process_data(month_list):
    for month in month_list:
        # Convert raw data file to pandas.DataFrame
        df = parse_df(find_month_data(month))
